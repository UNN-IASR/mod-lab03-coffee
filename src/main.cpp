// Copyright 2022 UNN-IASR
#include "Automata.h"

int main() {
	Automata automata;
	automata.on();
	automata.coin(200);
	automata.choice(2);
	automata.cook();
	automata.finish();
	automata.off();
	return 0;
}
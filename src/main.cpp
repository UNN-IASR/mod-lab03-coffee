// Copyright 2022 UNN-IASR
#include <iostream>
#include <string>
#include "Automata.h"

int main() {
    Automata automata;
    automata.on();
    automata.coin(140);
    automata.choice(3);
    automata.check();
    automata.cook();
    automata.finish();
    automata.off();
    return 0;
}
